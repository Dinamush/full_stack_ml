# Core Framework and Server
fastapi==0.95.2
uvicorn==0.22.0
gunicorn==20.1.0

# Database and ORM
sqlalchemy==1.4.46
alembic==1.7.7

# Authentication and Security
pydantic==1.10.2
pydantic[email]==1.10.2  # Matches core pydantic version for compatibility
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # Pinned to prevent compatibility issues with passlib
python-jose==3.3.0
email-validator==1.2.1

# Machine Learning
tensorflow==2.18.0  # Updated TensorFlow version for NumPy 2.x compatibility

# Optional: pybind11 for module builds with cross-compatibility for NumPy 2.x
pybind11>=2.12  # Uncomment if needed for specific modules requiring compatibility with NumPy 2.x
